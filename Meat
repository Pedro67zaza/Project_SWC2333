/**
 * Program purpose: To provide an interface of the Meat class to be utilized in the HomePageFrame
 *
 * @author (your name)
 * @version (a version number or a date)
 * 
 **/

import javax.swing.*;
import java.awt.*;
import java.awt.event.*;
import java.util.Random;

public class Meat extends JFrame implements Categories, ActionListener
{   
    private JLabel chicken, duck, salmon, beef, wagyu, lamb;
    private JPanel stockPanel;
    private JButton btnBack;
    private JFrame categoryFrame;
    private Random randomNum;

    @Override
    public void getContent() {
        //create the frame
        categoryFrame = new JFrame("Quantity");
        categoryFrame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        categoryFrame.setSize(500,500);
        categoryFrame.setLayout(new BorderLayout());
        categoryFrame.setVisible(true);
        //instantiate random object 
        randomNum = new Random();
        JLabel[] labels = {
            //create the labels
                new JLabel("Chicken: " + randomNum.nextInt(31)),
                new JLabel("Duck: " + randomNum.nextInt(31)),
                new JLabel("Salmon: " + randomNum.nextInt(31)),
                new JLabel("A5 Wagyu: " + randomNum.nextInt(31)),
                new JLabel("Beef: " + randomNum.nextInt(31)),
                new JLabel("Lamb: " + randomNum.nextInt(31)),
            };
        //create the go back button
        btnBack = new JButton("Go Back");
        btnBack.addActionListener(this);
        //create the panel 
        stockPanel = new JPanel();
        stockPanel.setLayout(new BoxLayout(stockPanel, BoxLayout.Y_AXIS));
        stockPanel.setBorder(BorderFactory.createEmptyBorder(10, 10, 10, 10));

        for (JLabel label : labels) {
            label.setFont(new Font("Arial", Font.PLAIN, 16)); // Set font and size
            label.setAlignmentX(Component.LEFT_ALIGNMENT); // Align labels to the left
            stockPanel.add(label);
            stockPanel.add(Box.createRigidArea(new Dimension(0, 10))); // Add spacing between labels
        }
        //add the button to the panel 
        stockPanel.add(btnBack);
        //add the panel to the frame 
        categoryFrame.add(stockPanel, BorderLayout.CENTER);
    }
    public void actionPerformed(ActionEvent e){
        categoryFrame.dispose();//close current Frame 
    }
}
